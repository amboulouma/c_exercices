#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <limits.h>

int main()
{
    unsigned int a = 0, b = -1;
    printf("min(int) = %d\nmax(int) = %d\nmin(unsigned int) = %u\nmax(unsigned int) = %u", INT_MIN, INT_MAX, a, b);
}