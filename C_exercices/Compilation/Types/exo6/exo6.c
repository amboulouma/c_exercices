#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <limits.h>

int main()
{
    char super = 17, bof = 10, naze = 8, superman = 22, sousleau = 2;
    printf("super = %x, bof = %x, naze = %d, superman = %x, souleau = %x", super, bof, naze, superman, sousleau);
}