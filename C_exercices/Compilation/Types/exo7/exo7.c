#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <limits.h>

int main()
{
    long i;
    long long j;
    printf("sizeof(long) = %ld, sizeof(long long) = %ld", sizeof(i), sizeof(j));
}